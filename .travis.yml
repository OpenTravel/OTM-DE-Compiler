language: java
jdk: oraclejdk8
install: true
script: mvn clean install
cache:
  directories:
  - $HOME/.m2
after_success:
  - bash <(curl -s https://copilot.blackducksoftware.com/ci/travis/scripts/upload)